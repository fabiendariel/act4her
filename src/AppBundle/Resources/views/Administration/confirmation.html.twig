{% extends "AppBundle::layout.html.twig" %}

{% block ocplatform_body %}

    <div class="row">
        <h1 class="col-md-11">{{ 'Création de votre mot de passe'|trans }}</h1>
    </div>

    {{ form_start(form, {'attr': {'novalidate': 'confiture', 'autocomplete': 'nope'}}) }}

        <div class="row margin-top-20">
            {{ app_form.form_row(form, 'password', 'col-md-6', 'Mot de passe') }}
            {{ app_form.form_row(form, 'password_confirm', 'col-md-6', 'Confirmation du mot de passe') }}
        </div>

        <div class="row margin-top-20 text-center">
            <input type="submit" value="{{ 'Valider'|trans }}" class="btn btn-lg btn-success" />
        </div>

    {{ form_end(form) }}

{% endblock %}